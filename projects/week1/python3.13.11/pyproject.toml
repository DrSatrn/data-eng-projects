[project]
name = "python3-13-11"
version = "0.1.0"
description = "segregated depn from local machine :)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.1",
    "pyarrow>=23.0.1",
]
